"""TEST FIXTURES ONLY.

Everything in this package is synthetic data used to exercise parsing and error
handling. It is never imported by application code — see ``test_fixtures_are_test_only``
in ``tests/test_trend_providers.py``, which enforces that.
"""
